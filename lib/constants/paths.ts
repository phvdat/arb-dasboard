import path from 'path';

export const FIXED_DATA_PATH = path.join(
  process.cwd(),
  'data',
  'fixed.json'
);

export const DYNAMIC_DATA_PATH = path.join(
  process.cwd(),
  'data',
  'dynamic.json'
);

export const META_PATH = path.join(
  process.cwd(),
  'data',
  'config.json'
);